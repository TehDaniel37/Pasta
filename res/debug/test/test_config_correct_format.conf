Module "echo_every_10_s"
    run `echo 'testing interval every 10 s'`
    every 10 s

Module "echo_every_10_sec"
    run `echo 'testing interval every 10 sec'`
    every 10 sec

Module "echo_every_10_seconds"
    run `echo 'testing interval every 10 seconds'`
    every 10 seconds

Module "echo_every_second"
    run `echo 'testing interval every second'`
    every second

Module "echo_every_5_m"
    run `echo 'testing interval every 5 m'`
    every 5 m

Module "echo_every_5_min"
    run `echo 'testing interval every 5 min'`
    every 5 m

Module "echo_every_5_minutes"
    run `echo 'testing interval every 5 minutes'`
    every 5 minutes

Module "echo_every_minute"
    run `echo 'testing interval every minute'`
    every minute

Module "echo_every_2_h"
    run `echo 'testing interval every 2 h'`
    every 2 h

Module "echo_every_2_hours"
    run `echo 'testing interval every 2 hours'`
    every 2 hours

Module "echo_every_hour"
    run `echo 'testing interval every hour'`
    every hour
